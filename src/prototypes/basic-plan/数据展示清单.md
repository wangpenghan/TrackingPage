# 数据展示清单

## 一、列车卡片数据展示状态

| 字段名 | 数据类型 | 是否已展示 | 展示位置 | 备注
|--------|----------|------------|----------|------
| 序号 | number | ✅ 隐含 | - | 通过卡片顺序
| 全车次 (fullTrainNo) | string | ✅ | 卡片顶部 | 与车次号合并
| 始发车次 (startingTrainNo) | string | ✅ | 卡片顶部 | 与车次号合并
| 始发站 (originStation) | string | ✅ | 卡片中部 | 始发终到区域
| 终到站 (destinationStation) | string | ✅ | 卡片中部 | 始发终到区域
| 车型 (trainModel) | string | ✅ | 卡片顶部 | 车型标签
| 编组 (formationCount) | number | ✅ | 卡片顶部 | 图号/编组/定员行
| 定员 (capacity) | number | ✅ | 卡片顶部 | 图号/编组/定员行
| 调向站 (turningStation) | string | ✅ | 卡片标签区 | 调向站标签
| 开行周期 (operationCycle) | number | ✅ | 开行规则标签 | 隐含在规则中
| 开行规则 (operationRule) | string | ✅ | 卡片顶部右侧 | 开行规则标签
| 交路信息 (routeInfo) | string | ✅ | 详情抽屉头部 | 交路信息
| 经过线路 (passingLines) | string[] | ✅ | 卡片标签区 | 线路标签
| 经过路局 (passingBureaus) | string[] | ✅ | 卡片底部 | 路局信息
| 备注 (remarks) | string | ✅ | 卡片标签区 | 备注标签
| 图号 (diagramNo) | string | ✅ | 卡片顶部 | 图号信息
| 开行类型 (operationRule) | string | ✅ | 卡片顶部 | 开行规则标签

---

## 二、站详情数据展示状态

| 字段名 | 数据类型 | 是否已展示 | 展示位置 | 备注
|--------|----------|------------|----------|------
| 序号 (stationOrder) | number | ✅ | 时间轴节点 | 站序号
| 站名 (stationName) | string | ✅ | 时间轴主体 | 站名
| 全车次 (fullTrainNo) | string | ✅ | 站详情信息 | 到达车次/出发车次
| 到达车次 (arrivalTrainNo) | string | ✅ | 站详情信息 | 到达车次
| 出发车次 (departureTrainNo) | string | ✅ | 站详情信息 | 出发车次
| 到达时间 (arrivalTime) | string | ✅ | 时间轴右侧 | 到达时间
| 发车时间 (departureTime) | string | ✅ | 时间轴右侧 | 发车时间
| 股道 (track) | string | ✅ | 站详情信息 | 股道
| 更新时间 (updateTime) | string | ✅ | 站详情信息 | 更新时间

---

## 三、总结

✅ **所有数据字段均已展示！**

当前实现已经包含了所有原始界面的数据字段，并且通过合理的布局和设计，信息层次分明，既饱满而不拥挤。

设计特点：
1. 重要信息优先展示（车次、始发终到、车型等）
2. 次要信息用标签或小字展示
3. 详细信息在详情抽屉中完整展示
